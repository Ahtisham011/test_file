import 'package:flutter/material.dart';

class Myhome extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(backgroundColor: Colors.amber, title: Text("Profile")),
      // body: Center(
      //   child: Text(
      //     "Ahtisham",
      //     style: TextStyle(
      //       fontSize: 42,
      //       color: const Color.fromARGB(255, 113, 147, 185),
      //     ),
      //   ),
      // ),
      // floatingActionButton: FloatingActionButton(
      //   onPressed: () {},
      //   child: Icon(Icons.verified_user_sharp),
      // ),

      // backgroundColor: const Color.fromARGB(255, 178, 230, 82),
//       body:Container(
// width: double.infinity,
// child: Column(),

//       )

body: Column(
children: [
Text("Welcome"),
Text("Ahtisham"),

]

),
    );
  }
}
